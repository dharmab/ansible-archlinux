[project]
name = "ansible-archlinux"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ansible>=11.2.0",
]

[dependency-groups]
dev = [
    "ansible-lint>=25.1.2",
]
